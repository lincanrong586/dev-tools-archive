# dev-tools-archive

内部研发工具归档仓。

## 目录

- `polygon_bench/`：PolygonSet 编码格式基准测试工具，包含 `pbuf`、`json_obj`、`json_arr`、`bin_fixed`、`bin_offset_varint`、`delta_varint` 的对比测试代码与说明。

## 使用说明

每个工具目录内包含各自的构建与运行说明，请进入对应子目录查看 `README.md`。
